{
  "name": "hitomilog-eleventy",
  "version": "1.0.0",
  "description": "Hitomi Seki's blog.",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "@11ty/eleventy-upgrade-help": "^1.0.1",
    "cross-env": "^7.0.2",
    "del": "^6.1.1",
    "gulp": "^5.0.0",
    "gulp-clean-css": "^4.3.0",
    "gulp-rename": "^2.0.0",
    "gulp-terser": "^2.0.1",
    "html-minifier": "^4.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "gulp dev && cross-env ELEVENTY_ENV=development eleventy --serve --port=138",
    "build": "gulp prod && cross-env ELEVENTY_ENV=production eleventy"
  },
  "author": "Hitomi Seki (https://hitomiseki.com)"
}
